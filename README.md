Four Part Writer


Go into this directory on the command line and run the following commands with python 3.11 installed:

1. pip3.11 install -r requirements.txt # (only need to do this the first time)

1. python3.11 app.py

You should see "Running on [IP]:[Port]". This will be localhost:5000 for most. Navigate to that link in a browser.
